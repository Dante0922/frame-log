# frame-log 다음 작업 할일

최종 업데이트: 2026-02-17
브랜치 기준: `feature/backend-mvp`

## 0. 현재 상태

- 백엔드 MVP API 구현 및 PR 생성 완료 (`#2`)
- 프론트-백엔드 실연동 테스트 완료
  - 홈/목록/상세/리뷰작성 플로우 정상
  - 주요 API 2xx, 리뷰 생성 201 확인

## 1. 우선순위 작업 (P0)

### 1) API 계약 최종 고정

- [ ] `POST /api/spots/{id}/reviews` 요청에서 `spotId` 제거 여부 확정
- [ ] 확정안 기준으로 프론트/백엔드/API 문서 동기화
- [ ] 최종 계약을 `API_SPEC.md`에 명시

완료 기준:
- 프론트 요청 DTO와 백엔드 Request DTO가 완전히 일치
- `API_SPEC.md` 예시 요청/응답이 실제 동작과 동일

### 2) 프론트 실서비스 모드 전환 준비

- [ ] 로컬 개발 기본값을 실 API 기준으로 정리 (`VITE_USE_MOCK_DATA=false`)
- [ ] `.env.development.example`를 실제 운영 흐름 기준으로 갱신
- [ ] Mock 의존 코드 제거 범위 정의

완료 기준:
- 신규 개발자가 `.env.development`만 만들면 바로 실 API 연동 가능

### 3) 에러/로딩 UX 보강

- [ ] 리뷰 작성 중 중복 submit 방지
- [ ] API 실패 시 사용자 메시지 일관화 (alert 최소화)
- [ ] 상세/목록/홈 로딩 실패 fallback UI 정리

완료 기준:
- 네트워크 실패 시 빈 화면 없이 안내 문구/재시도 경로 제공

### 4) 백엔드 테스트 보강

- [ ] 목록/리뷰 페이징 파라미터 경계값 테스트 (`size<=0`, `size>100`)
- [ ] 404/400 에러 응답 포맷 검증 테스트 추가
- [ ] 리뷰 작성 유효성(공백, 최대 길이) 케이스 확장

완료 기준:
- `backend` 테스트가 주요 정상/예외 시나리오를 커버

## 2. 다음 우선순위 (P1)

### 1) CI 파이프라인 구성

- [ ] GitHub Actions에 `backend` 테스트 단계 추가
- [ ] GitHub Actions에 `frontend` 빌드/타입체크 단계 추가

완료 기준:
- PR마다 프론트/백엔드 기본 검증 자동 실행

### 2) 로컬 실행 표준화

- [ ] `docker-compose`로 MySQL/Backend/Frontend 기본 실행 구성
- [ ] 루트 또는 각 서비스 README 실행 가이드 정리

완료 기준:
- 한 명령으로 로컬 실행 또는 명확한 2~3단계 실행 가능

## 3. 백로그 (P2)

- [ ] 리뷰 목록 UI 페이징 적용
- [ ] CORS Origin 환경변수 기반 관리
- [ ] 운영 로그 포맷/요청 추적(Trace ID) 기본 도입

## 4. 추천 실행 순서

1. API 계약 최종 고정 (`spotId` 정책 확정)
2. 프론트 실서비스 모드 전환
3. 백엔드/프론트 테스트 보강
4. CI 연결
5. docker-compose + 문서화
